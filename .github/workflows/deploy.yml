name: Deploy

on:
  workflow_run:
    workflows: [CI]
    branches: [main]
    types: [completed]

jobs:
  migrate:
    name: Apply DB migrations
    runs-on: ubuntu-latest
    # Only run when CI passed; the environment gate requires manual approval.
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    environment: production
    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Link Supabase project
        run: supabase link --project-ref peznyiujmroyvqeyront
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

      - name: Apply migrations
        # db push is idempotent — already-applied migrations are tracked and skipped.
        run: supabase db push
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}

  # ── EAS Update (OTA) ──────────────────────────────────────────────────────
  # Uncomment once you have configured EAS:
  #   1. npx expo install expo-updates
  #   2. eas update:configure   (run once locally, commits eas.json + app.json changes)
  #   3. Add EXPO_TOKEN to GitHub secrets (https://expo.dev → Account settings → Access tokens)
  #
  # ota-update:
  #   name: EAS OTA update
  #   runs-on: ubuntu-latest
  #   needs: migrate
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: 20
  #         cache: npm
  #         cache-dependency-path: apps/mobile/package-lock.json
  #     - run: npm ci --legacy-peer-deps
  #       working-directory: apps/mobile
  #     - uses: expo/expo-github-action@v8
  #       with:
  #         eas-version: latest
  #         token: ${{ secrets.EXPO_TOKEN }}
  #     - name: Publish OTA update
  #       run: eas update --branch production --message "Deploy ${{ github.sha }}"
  #       working-directory: apps/mobile
